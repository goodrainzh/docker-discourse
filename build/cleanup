#!/bin/bash
set -e
[ $DEBUG ] && set -x

source $BUILD_DIR/env

rm -rf /root/.bundle
rm -rf /root/.gem
rm -rf /var/lib/dpkg/info/*
rm -rf /var/lib/apt/lists/*
rm -rf /usr/share/doc/*
rm -rf /usr/local/lib/ruby/gems/2.3.0/cache/*
rm -rf /tmp/*
